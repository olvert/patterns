import React from 'react';
import Head from 'next/head';

const Home = (): JSX.Element => (
  <div>
    <Head>
      <title>Patterns</title>
    </Head>

    <main>
      <h1>
        Hello World
      </h1>
    </main>

    <footer>
    </footer>
  </div>
);

export default Home;
